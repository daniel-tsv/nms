spring.datasource:
  url: jdbc:postgresql://localhost:5432/notehub
  username: postgres
  password: postgres

spring.jpa:
  hibernate.ddl-auto: create-drop
  show-sql: true

notehub.security:
  jwt.token-expiration-millis: 604800000 # 1 week
